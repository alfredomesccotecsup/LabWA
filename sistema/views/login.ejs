<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio de Sesión</title>

    <!-- Remix Icons -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2);
            padding: 40px;
            width: 400px;
            text-align: center;
            animation: scaleIn 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55);
        }

        @keyframes scaleIn {
            from {
                transform: scale(0.8);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form h2 {
            font-size: 28px;
            margin-bottom: 10px;
            color: #007bff;
        }

        label {
            text-align: left;
            margin-bottom: 10px;
            font-weight: bold;
            color: #555;
        }

        .pass-reset input {
            width: 100%;
            padding: 10px;
            border: none;
            border-bottom: 2px solid #007bff;
            background: transparent;
            color: #555;
            font-size: 16px;
            transition: border-color 0.3s ease-in-out;
        }

        .pass-reset input:focus {
            border-color: #0056b3;
        }

        .remember-box {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            margin-top: 10px;
        }

        .remember-text {
            margin-left: 10px;
            color: #555;
        }

        .btn-login {
            background-color: #007bff;
            color: #fff;
            padding: 12px 24px;
            border: none;
            border-radius: 30px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
            margin-top: 20px;
            transform: translateY(0);
        }

        .btn-login:hover {
            background-color: #0056b3;
            transform: translateY(-5px);
        }

        .or {
            font-size: 20px;
            margin: 20px 0;
            color: #555;
        }

        .btn-gogle-auth {
            background-color: #4285f4;
            color: #fff;
            padding: 12px 24px;
            border: none;
            border-radius: 30px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
            margin-top: 10px;
            transform: translateY(0);
        }

        .btn-gogle-auth:hover {
            background-color: #0c7a02;
            transform: translateY(-5px);
        }

        a {
            color: #007bff;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease-in-out;
        }

        a:hover {
            color: #0056b3;
        }
    </style>
</head>

<body>
    <div class="container">
        <img src="img/login.jpg" alt="img" class="img">
        <!-- Formulario de inicio de sesión -->
        <form action="/matriculas" method="post" class="form active" id="login">
            <h2>Iniciar Sesión</h2>
            <label for="usuario">Usuario</label>
            <div class="pass-reset">
                <input type="text" name="usuario" class="usuario" id="usuario" placeholder="usuario" required>
            </div>
            <label for="password">Contraseña</label>
            <div class="pass-reset">
                <input type="password" class="password" name="contrasena" id="contrasena" placeholder="contraseña" required>
            </div>
            <div class="pass-reset remember-box">
                <input type="checkbox" name="remember" class="remember" id="remember"><span class="remember-text">Recordar contraseña</span>
            </div>
            <button class="btn btn-login" type="submit">Iniciar Sesión</button>
            <p>¿No tienes una cuenta? <a onclick="changeToRegister()" id="changeToRegister">Regístrate</a></p>
            <p class="or">o</p>
            <button class="btn btn-gogle-auth"><i class="google ri-google-fill"></i>Iniciar Sesión con Google</button>
        </form>
    </div>
</body>

</html>
